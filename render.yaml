services:
  - type: web
    name: final-report-app
    env: python
    region: oregon
    plan: free
    pythonVersion: 3.11
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT backend.wsgi:app
    healthCheckPath: /
